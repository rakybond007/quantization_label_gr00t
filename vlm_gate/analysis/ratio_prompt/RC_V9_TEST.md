# robocasa v9 후보 시험 — v8 대비 (Gemini, 국면 정답 380타일)

v9 = v8 + 둘. **F 추가**(쥔 채 내려놓는 순간, 부호 −1) · **D 에서 `or about to` 제거**.
A·B·C·E 글, GUIDANCE, 척도, 이미지 장수, 순서는 v8 과 같다.

표본은 사람이 찍은 국면 정답 762타일 중 380개(시드 0). 접근 126 · 운반 88 · 파지 85 ·
해제 81. **해제는 그리퍼 상태로 둘로 가른다** -- 사람 라벨은 "쥔 채 내려놓는 중" 과
"이미 놓음" 을 하나로 묶는데 실측에서는 place_precise(1.25) 와 place_open(3.13) 이
2.5배 갈린다. F 가 겨냥하는 것이 앞쪽이다.

Gemini 3.8 Flash(low) · 팔 둘 합쳐 $3.13 · 5.8분 · 파싱실패 0.

## 무엇이 달라졌나

```
해제(쥔채) conf   v8 0.611  ->  v9 0.533    이제 해제(놓음) 0.572 아래 (맞는 순서)
D 등급            v8 4.29   ->  v9 3.11     `or about to` 제거가 들었다
F 등급 (v9)       해제(쥔채) 2.89 · 해제(놓음) 2.17 · 접근/운반/파지 1.1~1.4
```

F 가 겨눈 자리에서만 켜진다.

## 가중을 빼고 본 결론 (이게 핵심이다)

잠정 가중으로 낸 conf 는 믿을 수 없다. 부호만 물리에 고정하고(A·B·F 감점 ·
C·D·E 가산) **문항 집합이 실측 국면 순서를 표현할 수 있는지** 선형으로 풀었다.

| | 실측 5국면 순서 재현 | 부호 고정 최적합 rho |
|---|---|---|
| v8 | **불가능** | +0.000 |
| **v9** | **가능** | **+0.900** |

v8 은 어떤 말이 되는 가중으로도 못 만든다. v9 는 만들 수 있고, 최적합이 **F 에
−1.00**(최대 감점)을 준다 -- 빈자리를 실제로 메우고 있다.

## 남은 문제 — `접근` 은 둘 다 틀린다

`approach` 의 실측 상한이 **4.00 으로 전체 최고**인데 conf 는 가장 낮은 축이다
(v8 0.456 · v9 0.507). A 가 접근에서 2.93~3.01 로 켜지기 때문이다 -- "그리퍼가 한
지점에 닫혀야 하는가" 가 **파지하러 가는 동안**에도 참으로 읽힌다.

v9 최적합도 접근을 0.10 으로 운반(0.53) 아래에 둔다. **v7 부터의 문제이고 v9 가
건드리지 않았다.** 고치려면 A 를 "지금 닫히는 중" 으로 좁혀야 하는데, 그러면 A 와 B 의
관계가 바뀌므로 따로 재야 한다.

## 한계

- 국면이 다섯이라 rho 의 점이 다섯 개다. 오차를 못 붙인다.
- v9 의 현재 가중은 내가 대충 넣은 잠정값이다(A .40 B .27 F .33). 그 가중으로는
  아직 정밀<너그러움 검사를 통과하지 못한다. 문항 집합은 통과할 수 있다.
  **가중은 라벨링 뒤에 잡는다(R5).**

## 재현

```
CHECKS=phase9_checks_v8 RC_N=380 python scripts/robocasa_gemini_probe.py out8.jsonl
CHECKS=phase9_checks_v9 RC_N=380 python scripts/robocasa_gemini_probe.py out9.jsonl
```
