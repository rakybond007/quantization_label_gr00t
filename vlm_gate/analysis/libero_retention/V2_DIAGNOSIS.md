# libero v2 문항 진단 (국면균등 1,380 장면 · 40셀 · gp 100%)

`docs/QUESTION_DESIGN_HARNESS.md` 의 절차를 LIBERO 에 적용한 첫 결과.
robocasa 와 **다른 그림**이 나왔다.

## 부호는 맞다 -- robocasa v1 같은 반전이 없다

```
유지율 상관   1.53x +0.150 (p=0.356)
             1.76x +0.429 (p=0.006)
             1.80x +0.454 (p=0.003)
```

빠른 배속 둘에서 유의하게 양수다. 1.53x 가 약한 것은 그 배속이 거의 공짜(유지율
0.990)라 예측할 것이 없기 때문이다. **robocasa v1 은 -0.705 였다.**

## 태스크 수준에서 잘 작동한다

| suite | conf | A | B | **C** | D | E | 실측 유지율 1.80x |
|---|---|---|---|---|---|---|---|
| `libero_object` (전부 바구니) | **0.570** | 2.88 | 2.14 | **4.56** | 1.98 | 1.06 | **0.837** |
| `libero_spatial` (전부 접시) | **0.341** | 3.58 | 2.29 | **1.82** | 2.40 | 1.11 | **0.416** |

C(CATCHER)가 바구니 4.56 대 접시 1.82 로 **2.73 등급**을 가르고, conf 가 실측 유지율과
같은 방향이다. **LIBERO 에서는 압축 강건성이 실제로 태스크(놓는 곳의 성질)로 결정된다**
-- 태스크몫 0.949 는 결함이 아니라 데이터의 성질이다. robocasa 와 정반대다.

## 프레임별 분별력은 없다

|  | libero v2 | robocasa v7 |
|---|---|---|
| 태스크몫 | 0.949 | 0.653 |
| 국면몫 | 0.489 | 0.708 |
| 국면별 conf 폭 | **0.411~0.463 (0.05)** | 0.222~0.403 (0.18) |

## 문항별

| 문항 | 4등급 이상 | 최고 국면 | 의도 | 국면간 std |
|---|---|---|---|---|
| A SMALL_TARGET | 26% | 해제 3.61 | 내려놓기 | **0.284** |
| **B CROWDED_PICK** | **3%** | 운반 2.22 | 파지 ✗ | 0.145 |
| C CATCHER | 38% | 해제 2.84 | 해제 ✓ | 0.096 |
| **D WIDE_SURFACE** | **5%** | 접근 2.32 | 내려놓기 ✗ | 0.115 |
| **E NO_HOLD** | **2%** | 접근 1.37 | -- | 0.116 |

**셋(B·D·E)이 사실상 죽어 있다.** robocasa D 가 719/719 전부 1등급이던 것과 같은
증상이다 -- 문구가 이 데이터셋 화면과 안 맞는다. "물체보다 훨씬 큰 면"(D)은 LIBERO
에서 탁자 상판뿐인데 태스크가 거의 다 접시·바구니·서랍으로 간다.

**A 와 C 가 같은 국면에서 상쇄한다.** 둘 다 해제에서 최고인데 부호가 반대다
(A 감점 3.61 · C 가점 2.84). 그래서 국면별 conf 가 평평해진다.

## v3 시험

한 판에 한 문항만 바꾼다(R2).

  v3a  D 를 "손을 떼는 순간인가" 로 -- robocasa 에서 해제 1.34 -> 3.51 로 살린 문구
  v3b  B 를 "파지 순간인가" 로

재현: `python scripts/libero_gp_check.py` + `scripts/libero_scorecard.py`.
채점표 원본은 `score_v2.txt`.
